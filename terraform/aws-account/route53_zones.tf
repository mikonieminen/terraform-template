# Define zones here
# Usually initial definition of a new zone requires
# manual steps when registering nameservers to the
# system where the domain is bought. That's why it
# is better to define zones at the account size while
# infrastrucutre side defines subdomains and routing.

# resource "aws_route53_zone" "root" {
#   name = "example.com"
# }
